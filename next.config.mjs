/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    
  },
  typescript: {
    
  },
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  skipTrailingSlashRedirect: true,
}

export default nextConfig
